# go-identicon

go-identicon is a Go library for generating identicons from strings.

It provides a simple way to create deterministic, visually distinct identicon images, with PNG output supported.

## Demo

```
go run cmd/demo xxx
```
